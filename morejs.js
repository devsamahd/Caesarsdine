const callapi = (charactername) => {
    //don't forget access token
    fetch(``)
    .then(response=> response.json())
    .then(data => {
        data.
    })
}
const callapi = (charactername) => {
    //don't forget access token
    fetch(``)
    .then(response=> response.json())
    .then(data => {
        data.
    })
}
const callapi = (charactername) => {
    //don't forget access token
    fetch(``)
    .then(response=> response.json())
    .then(data => {
        data.
    })
}